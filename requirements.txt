# Runtime dependencies for the web product (Python 3.12).
# Install:  pip install -r requirements.txt
fastapi
uvicorn
numpy
pandas
scipy
PyMuPDF>=1.27,<2        # reads the 2D floor plans (needed to start the server + the upload flow)

# Optional — only for the legacy Streamlit dashboard under dashboard/ (superseded by the web app):
#   streamlit>=1.56,<2
